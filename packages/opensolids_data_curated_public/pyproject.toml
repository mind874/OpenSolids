[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "opensolids-data-curated-public"
version = "0.3.4"
description = "Curated public-source data pack for OpenSolids"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/opensolids_data_curated_public"]
